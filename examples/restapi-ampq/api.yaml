main: restapi
modules: 
  - module: restapi
  - !include amqp.config.yaml
steps:
  - id: queue
    use: rabbitmq
    input:
      message: !eval main
  - return:
      status_code: 201
      body: !eval steps.queue
      headers:
        Content-Type: application/json
