main: http_server
modules:
  - module: http_server
    with:
      port: 4000
steps:
  - return:
      status_code: 200
      body: 
        message: !eval main.uri
      headers:
        Content-Type: application/json
