main: restapi
modules: !include modules.yaml
steps:
  - id: echo
    use: echo
    input:
      message: Hello, World!
  - return:
      status_code: 201
      body: !include assets/body.yaml
      headers:
        Content-Type: application/json
