[package]
name = "anyflow-http"
version = "0.1.0"
edition = "2021"

[dependencies]
valu3 = { version = "0.8.2", features = ["default", "cstring"] }
sdk = { path = "../../sdk" }

[lib]
name = "http"
crate-type = ["cdylib"]
