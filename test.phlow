modules:
  - module: log

steps:
  - assert: true
    then:
      - payload: !phs payload + 1
      - use: log
        input:
          message: !phs payload + 1

  - payload: !phs payload + 10
  - payload: !phs payload + 1
  - use: log
    input:
      level: error
      message: !phs payload

tests:
  - describe: Test
    payload: 1
    assert_eq: 13
