modules:
  - module: log

steps:
  - assert: true
  - payload: !phs main
  - assert: true
    then:
      - assert: true
      - payload: !phs payload + 1
      - use: log
        input:
          message: !phs payload + 1

  - payload:  !phs payload + 10
  - use: log
    input:
      level: error
      message: !phs payload + 1

tests:
  - describe: Test
    main: 1
    assert_eq: 14
